/** Sanity project — used by Studio config (must be valid: a-z, 0-9, dashes only). */
export const sanityProjectId =
  process.env.NEXT_PUBLIC_SANITY_PROJECT_ID || "s5smijy4";

export const sanityDataset =
  process.env.NEXT_PUBLIC_SANITY_DATASET || "production";
